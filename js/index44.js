const text = 'HELLO';

const result = `${text.charAt(4)}${text.charAt(3)}${text.charAt(2)}${text.charAt(1)}${text.charAt(0)}`;
const resultTransform = result.toLocaleLowerCase();


console.log(`the final result is: ${resultTransform}`);







